import 'package:flutter/material.dart';

class Remland extends StatelessWidget {
  const Remland({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Remland'),
      ),
    );
  }
}