import 'package:flutter/material.dart';

class Starseed extends StatelessWidget {
  const Starseed({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Starseed'),
      ),
    );
  }
}